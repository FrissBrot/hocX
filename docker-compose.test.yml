# Dedicated test-host override. Intentionally empty for now: test runs the same release
# stack as prod, all environment-specific differences come from .env and the compose
# project name (`hocx-test`).
services: {}
